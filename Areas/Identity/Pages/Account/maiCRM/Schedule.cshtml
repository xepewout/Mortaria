@page
@model Mortaria.Areas.Identity.Pages.Account.maiCRM.ScheduleModel
@{
    ViewData["Title"] = "Schedule";
}

<h2>Schedule</h2>

<table class="table">
    <thead>
        <tr>
            <th>Customer</th>
            <th>Date</th>
            <th>Time</th>
            <th>Description</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var appointment in Model.Appointments)
        {
            <tr>
                <td>@appointment.Customer.AccountName</td>
                <td>@appointment.Date.ToShortDateString()</td>
                <td>@appointment.Time</td>
                <td>@appointment.Description</td>
            </tr>
        }
    </tbody>
</table>

<div class="mb-3">
    <a class="btn btn-primary" asp-page="./AddAppointment">Add Appointment</a>
    <a class="btn btn-secondary" asp-page="./AddCustomer">Add Customer</a>
</div>
